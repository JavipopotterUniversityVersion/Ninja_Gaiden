using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ModificarTag : MonoBehaviour
{
    public string tagUno = "Golpe";
    public string tagDos = "GolpeFuerte";
    public Transform padre;

    Enemigo enemy;

    private void Awake()
    {
     
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
